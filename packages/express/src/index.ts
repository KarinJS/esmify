import * as express from 'express'
const {
  application,
  request,
  response,
  // @ts-ignore
  Route,
  Router,
  json,
  raw,
  text,
  urlencoded,
} = express

export {
  application,
  request,
  response,
  // @ts-ignore
  Route,
  Router,
  json,
  raw,
  text,
  urlencoded,
  express as default
}
